<UserControl x:Class="MwccInspectorUI.View.SnapshotListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MwccInspectorUI.View"
             xmlns:viewmodel="clr-namespace:MwccInspectorUI.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:SnapshotListViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="200">
    <ListView
        ItemsSource="{Binding Snapshots}"
        DisplayMemberPath="FunctionName"
        BorderThickness="0"
        SelectedItem="{Binding SelectedSnapshot}"
    />
</UserControl>
