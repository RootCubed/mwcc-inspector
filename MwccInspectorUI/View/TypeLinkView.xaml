<UserControl x:Class="MwccInspectorUI.View.TypeLinkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MwccInspectorUI.View"
             xmlns:viewmodel="clr-namespace:MwccInspectorUI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TextBlock>
        <Hyperlink
            Command="{x:Static viewmodel:SnapshotCommands.TypeClicked}"
            CommandParameter="{Binding}">
            <TextBlock Text="{Binding}" Margin="0" />
        </Hyperlink>
    </TextBlock>
</UserControl>
