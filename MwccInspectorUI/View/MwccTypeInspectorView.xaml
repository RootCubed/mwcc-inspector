<UserControl x:Class="MwccInspectorUI.View.MwccTypeInspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MwccInspectorUI.View"
             xmlns:viewmodel="clr-namespace:MwccInspectorUI.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:MwccTypeInspectorViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <TreeViewItem Header="{Binding Name}">
        <ItemsControl ItemsSource="{Binding Children}">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type viewmodel:MwccTypeInspectorViewModelLeaf}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="=" />
                        <TextBlock Text="{Binding DisplayValue}" />
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodel:MwccTypeInspectorViewModel}">
                    <local:MwccTypeInspectorView DataContext="{Binding}" />
                </DataTemplate>
            </ItemsControl.Resources>
        </ItemsControl>
    </TreeViewItem>
</UserControl>
