<UserControl x:Class="MwccInspectorUI.View.MwccTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MwccInspectorUI.View"
             xmlns:viewmodel="clr-namespace:MwccInspectorUI.ViewModel"
             xmlns:converters="clr-namespace:MwccInspectorUI.ViewModel.Converters"
             xmlns:mwcctypes="clr-namespace:MwccInspector.MwccTypes;assembly=MwccInspector"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=mwcctypes:TypeBase}"
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontWeight="Bold" Text="Type" />
        <TextBlock Grid.Column="1" Text="{Binding Type}" />
        <TextBlock FontWeight="Bold" Grid.Row="1" Text="Size" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Size}" />
        <ContentControl Grid.Row="2" Grid.ColumnSpan="2" Content="{Binding}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type mwcctypes:TypeBasicType}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Basic Type" FontWeight="Bold" Grid.Column="0" />
                        <TextBlock Text="{Binding}" Grid.Column="1" />
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type mwcctypes:TypePointer}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Pointer to" FontWeight="Bold" Grid.Column="0" />
                        <local:TypeLinkView Grid.Column="1" DataContext="{Binding TargetType}" />
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type mwcctypes:TypeClass}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Namespace hierarchy" FontWeight="Bold" Grid.Column="0" />
                        <ItemsControl Grid.Column="1" VerticalAlignment="Center" MaxHeight="200" ItemsSource="{Binding NameSpace.Hierarchy}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <local:InlineHashNameNodeView DataContext="{Binding Name}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="Name" FontWeight="Bold" Grid.Row="1" />
                        <local:InlineHashNameNodeView Grid.Row="1" Grid.Column="1" DataContext="{Binding ClassName}" />
                        <TextBlock Text="Base classes" FontWeight="Bold" Grid.Row="2" Grid.ColumnSpan="2" />
                        <DataGrid Grid.Row="3" Grid.ColumnSpan="2"
                                  PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                                  ItemsSource="{Binding BaseClasses}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Name" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:TypeLinkView DataContext="{Binding Base}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Offset" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock VerticalAlignment="Center" Text="{Binding Offset}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="Member variables" FontWeight="Bold" Grid.Row="4" Grid.ColumnSpan="2" />
                        <DataGrid Grid.Row="5" Grid.ColumnSpan="2"
                                  PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                                  ItemsSource="{Binding MemberVars}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Type" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:TypeLinkView DataContext="{Binding Type}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Name" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:InlineHashNameNodeView DataContext="{Binding Name}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Access" Width="Auto" Binding="{Binding Access, Converter={StaticResource AccessTypeConverter}}" />
                                <DataGridTextColumn Header="Offset" Width="Auto" Binding="{Binding Offset}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type mwcctypes:TypeFunc}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Return Type" FontWeight="Bold" Grid.Column="0" />
                        <local:TypeLinkView Grid.Column="1" DataContext="{Binding FuncType}" />
                        <TextBlock Text="Arguments" FontWeight="Bold" Grid.Row="1" Grid.ColumnSpan="2" />
                        <DataGrid Grid.Row="2" Grid.ColumnSpan="2"
                                  PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                                  ItemsSource="{Binding FuncArgs}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Argument Name" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:InlineHashNameNodeView DataContext="{Binding Name}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Argument Type" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:MwccTypeView DataContext="{Binding Type}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type mwcctypes:TypeVoid}">
                </DataTemplate>
                <DataTemplate DataType="{x:Type mwcctypes:TypeBase}">
                    <DataTemplate.Resources>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}" />
                    </DataTemplate.Resources>
                    <TextBlock Text="{Binding ., StringFormat=Unhandled {0}}" Grid.Column="1"/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
